# Rule index

One convention per file in `docs/rules/`. **Route from this table, read only what it
names, stop.** Reading the corpus is a bug, not diligence.

Each rule file starts with YAML frontmatter:

```yaml
---
id: error-envelope                    # filename without .md
title: ...                            # one line
applies_to: ["app/controllers/**"]    # globs — if you are editing a match, the rule applies
triggers: ["problem+json", "422"]     # grep these when routing by symptom
see_also: ["status-codes"]            # other rule ids
modes: [ web, api ]                   # which app this rule ships to
tokens: 830                           # read cost, generated by bin/doc-tokens
current_state: matches                # or `diverges`, when the code does not obey it yet
---
```

So `grep -l "<keyword>" docs/rules/*.md` finds a rule by symptom without loading
anything.

Know the symptom but not the file? [`SYMPTOMS.md`](SYMPTOMS.md) — the symptom table and
the full annotated list. It costs more than this page; come here first.

## Route by path

Editing a file that matches → read these rules.

| Path | Rules |
|---|---|
| `config/routes.rb` | [controllers](controllers.md) · [api-versioning](api-versioning.md) |
| `app/controllers/api/**` | [controllers](controllers.md) · [api-auth](api-auth.md) · [error-envelope](error-envelope.md) · [policy-objects](policy-objects.md) — then the controller table below |
| `app/serializers/**` | [serialization](serialization.md) — then the serializer table below |
| `app/contracts/**` | [request-contracts](request-contracts.md) |
| `app/filters/**` | [filtering-sorting](filtering-sorting.md) · [cursor-pagination](cursor-pagination.md) |
| `app/services/**` | [service-objects](service-objects.md) · [jobs](jobs.md) |
| `app/queries/**` | [query-objects](query-objects.md) · [n-plus-one](n-plus-one.md) |
| `app/policies/**` | [policy-objects](policy-objects.md) |
| `app/lib/**` | [registries](registries.md) |
| `app/models/**` | [scopes](scopes.md) · [callbacks](callbacks.md) · [n-plus-one](n-plus-one.md) — then the model table below |
| `app/models/concerns/**` | [optional-patterns](optional-patterns.md) |
| `app/jobs/**`, `config/queue.yml`, `config/recurring.yml` | [jobs](jobs.md) · [async-202](async-202.md) |
| `config/initializers/doorkeeper.rb` | [api-auth](api-auth.md) |
| `config/initializers/cors.rb` | [cors](cors.md) |
| `config/initializers/rack_attack.rb` | [rate-limiting](rate-limiting.md) |
| `db/migrate/**` | [safe-migrations](safe-migrations.md) · [database-conventions](database-conventions.md) · [seeds](seeds.md) |
| `db/seeds.rb`, `db/seeds/**` | [seeds](seeds.md) |
| `test/**` | [api-testing](api-testing.md) · [testing](testing.md) — then the test table below |
| `openapi.yaml` | [openapi-contract](openapi-contract.md) · [client-contract](client-contract.md) |
| Anything under `app/`, when you are unsure which layer calls which | [write-path](write-path.md) |
| Adding a new directory under `app/` | [pattern-budget](pattern-budget.md) · [rejected-patterns](rejected-patterns.md) |

## Then, only what you are actually doing

The rows above are what applies to *any* edit in that path. These are conditional —
read one when the work touches it, not because you opened a file of that type.

| Editing a controller, and you are… | Also read |
|---|---|
| writing an index or any list action | [cursor-pagination](cursor-pagination.md) · [filtering-sorting](filtering-sorting.md) |
| accepting a request body | [request-contracts](request-contracts.md) |
| choosing what to answer with | [status-codes](status-codes.md) |
| rescuing, or loading a record by id | [exception-boundary](exception-boundary.md) |
| writing something that costs money or calls a provider | [idempotency](idempotency.md) |
| starting work that outlives the request | [async-202](async-202.md) |
| taking more than one record in a call | [bulk-endpoints](bulk-endpoints.md) |
| touching an endpoint a stranger can reach | [rate-limiting](rate-limiting.md) |
| retiring an endpoint, or changing what one returns | [deprecation-policy](deprecation-policy.md) · [api-versioning](api-versioning.md) |
| adding a cache, or answering a conditional GET | [caching](caching.md) |
| reaching for `Current.user` or any ambient state | [current-attributes](current-attributes.md) |

| Editing a serializer, and you are… | Also read |
|---|---|
| adding a field not every client wants | [sparse-fieldsets-includes](sparse-fieldsets-includes.md) |
| exposing an association | [sparse-fieldsets-includes](sparse-fieldsets-includes.md) · [n-plus-one](n-plus-one.md) |
| renaming, retyping, or removing a field | [api-versioning](api-versioning.md) · [deprecation-policy](deprecation-policy.md) |
| deciding what the client may rely on | [client-contract](client-contract.md) |

| Editing a model, and you are… | Also read |
|---|---|
| adding a destroy path, or reaching for a soft delete | [deletes](deletes.md) |
| changing money, permissions, or a status column | [audit-trail](audit-trail.md) · [optional-patterns](optional-patterns.md) |
| changing what a fresh checkout needs | [seeds](seeds.md) |
| adding a fixed set of variants with attributes | [registries](registries.md) |

| Editing tests, and you are… | Also read |
|---|---|
| adding or changing an endpoint | [openapi-contract](openapi-contract.md) |
| asserting a failure | [error-envelope](error-envelope.md) · [status-codes](status-codes.md) |
| testing who may call what | [api-auth](api-auth.md) · [policy-objects](policy-objects.md) |

**Read cost:** this index (~1600) plus one or two rules (220–1150) ·
[`SYMPTOMS.md`](SYMPTOMS.md) (~2400) only when routing by path missed. Every `tokens`
figure is generated from the file by `bin/doc-tokens` — budget from it, and re-run that
script after editing a rule.

Adding, renaming, or deleting a rule means updating this table **and**
[`SYMPTOMS.md`](SYMPTOMS.md); `id` and filename must match, and `modes` decides which
index it belongs in. Decisions live in [`../adr/`](../adr/), not here.
